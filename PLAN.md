## 오늘의 성공
배포된 URL에서 지금까지 총 몇 문제를 풀었는지와, 오늘 푼 문제 정리를 볼 수 있다

## 스켈레톤
이미 배포된 문제풀이 화면(선택→제출→해설)이 최소 경로 역할을 함. 별도 더미 스켈레톤은 생략하고, 기능 1(결과 저장)부터 바로 슬라이스로 진행

## 오늘 만들 기능
1. 사용자가 문제를 풀면 결과가 저장되어, 새로고침해도 남아있다 — ✅ 완료
2. 사용자가 첫 화면에서 시험을 선택할 수 있다 (SQLD만 실제 동작, 나머지는 "준비중") — ✅ 완료 (세션 중간에 추가된 요청)
3. 사용자가 왼쪽에서 메인 흐름과 브랜치(더 풀기로 갈라진 문제들)를 한눈에 볼 수 있다 — ✅ 완료 (세션 중간에 우선순위가 당겨져 3·4보다 먼저 만듦)
4. 사용자가 지금까지 총 몇 문제를 풀었는지와 정답률을 볼 수 있다 — 남음
5. 사용자가 오늘 푼 문제와 해설을 모아서 다시 볼 수 있다 — 남음

> 원래 3개 제한이었는데 세션 중간에 요청이 두 번 더 들어와서 5개로 늘어남. 대신 콘텐츠는 SQLD만 실제로 만들고, 그래프도 픽셀 정밀 대신 들여쓰기 트리로 단순화해서 범위는 최대한 작게 유지 (아래 "안 만들 것" 참고)

## 완료 조건

**기능 1**
- [ ] 문제를 제출하면 결과(문제 id, 정답 여부, 시각)가 저장된다
- [ ] 새로고침해도 지금까지 푼 기록이 사라지지 않는다
- [ ] 브라우저를 껐다 켜도 기록이 남아있다 (localStorage)

**기능 2 (시험 선택 화면)**
- [ ] 첫 화면에서 SQLD, SQLP, 토익, 리액트, Next.js, C언어 등 시험 카드 목록이 보인다
- [ ] SQLD 카드를 누르면 기존 문제풀이 화면으로 이동한다
- [ ] SQLD 외 카드는 "준비중" 표시만 있고 눌러도 문제풀이로 안 들어가진다 (실제 콘텐츠 없음)

**기능 3 (브랜치 시각화)**
- [ ] 왼쪽 사이드바에 메인 흐름(1~20)과 지금 위치가 보인다
- [ ] "더 풀기"로 갈라진 브랜치가 스폰된 지점 아래 들여써져서 보인다
- [ ] 브랜치 안에서 또 "더 풀기"를 누르면 중첩 브랜치가 보인다

**기능 4**
- [ ] 화면에 "지금까지 총 N문제 풀었음"이 보인다
- [ ] 정답률(%)이 함께 보인다
- [ ] 문제를 하나 더 풀면 숫자가 바로 갱신된다

**기능 5**
- [ ] "오늘 푼 문제" 화면에서 오늘 날짜에 푼 문제만 보인다
- [ ] 각 문제의 정답 여부와 해설을 다시 볼 수 있다
- [ ] 오늘 아무것도 안 풀었으면 빈 상태 문구가 보인다

## 오늘 안 만들 것
- 로그인 / 회원가입
- 결제
- 관리자 페이지 (문제 추가·수정 UI)
- 알림
- 기기 간 진행 상황 동기화 (서버 저장 없음, 브라우저 로컬에만)
- SQLD 외 시험(SQLP·토익·리액트·Next.js·C언어 등)의 실제 문제 콘텐츠 제작 — 화면 카드만 만들고 "준비중"으로 둠, 콘텐츠 확장은 다음 세션
- 디자인/레이아웃 다듬기

## 걱정되는 것
localStorage는 브라우저·기기별로 따로 저장돼서 "총 몇 문제 풀었는지"가 기기를 바꾸면 초기화된다. 로그인이 없는 지금 범위에서는 감수하기로 함.
